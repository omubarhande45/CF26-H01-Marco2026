# Copy to repo root as .env before: docker compose -f deploy/docker-compose.yml up -d

FCQF_ENV=production
JWT_SECRET=replace-with-64-plus-random-chars
JWT_ALG=HS256
AUTH_PROVIDER=local
ALLOW_DEMO_USERS=1

USER_RESEARCHER_PASSWORD=research123
USER_AUDITOR_PASSWORD=audit123
USER_ADMIN_PASSWORD=admin123

SERVICE_AUTH=token
SERVICE_TOKEN=replace-with-another-long-random-string
REQUIRE_SERVICE_AUTH=0

MIN_COHORT=10
ALLOWED_ORIGINS=http://localhost,https://cf-26-h01-marco2026-one.vercel.app

# Internal compose DNS — do not change unless you rename services
NODE_A=http://hospital_a:8101
NODE_B=http://hospital_b:8102
NODE_L=http://diagnostic_lab:8103
